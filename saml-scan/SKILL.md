---
name: saml-scan
description: >
  Scans codebase and updates memory files (`AGENTS.md` and `agents/*.md`) with the latest info.
  This should be triggered automatically every time you scan codebase.
---

# Process

1. Explore the codebase if you haven't already.

3. Update `AGENTS.md` or any files that appear in `AGENTS.md` (mostly `agents/*.md` files) to match the current codebase.
